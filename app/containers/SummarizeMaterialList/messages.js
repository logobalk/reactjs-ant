/*
 * SummarizeMaterialList Messages
 *
 * This contains all the text for the SummarizeMaterialList container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SummarizeMaterialList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Summarize Material List',
  },
});
