/*
 * InspectionList Messages
 *
 * This contains all the text for the InspectionList container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.InspectionList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the InspectionList container!',
  },
});
