/*
 * MaterialIndentificationList Messages
 *
 * This contains all the text for the MaterialIndentificationList container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MaterialIndentificationList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Material Identification List',
  },
});
