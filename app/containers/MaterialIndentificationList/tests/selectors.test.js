// import { fromJS } from 'immutable';
// import { selectMaterialIndentificationListDomain } from '../selectors';

describe('selectMaterialIndentificationListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
