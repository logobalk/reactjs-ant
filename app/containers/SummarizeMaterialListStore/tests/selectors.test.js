// import { fromJS } from 'immutable';
// import { selectSummarizeMaterialListStoreDomain } from '../selectors';

describe('selectSummarizeMaterialListStoreDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
