// import { fromJS } from 'immutable';
// import { selectDispensingValidationDomain } from '../selectors';

describe('selectDispensingValidationDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
