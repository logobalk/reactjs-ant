/*
 * MaterialGroupList Messages
 *
 * This contains all the text for the MaterialGroupList container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MaterialGroupList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Sampling Checking',
  },
});
