/*
 * MaterialIdentityLabel Messages
 *
 * This contains all the text for the MaterialIdentityLabel container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MaterialIdentityLabel';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Material Loading Process',
  },
  description: {
    id: `${scope}.description`,
    defaultMessage: 'Identity Label',
  },
});
