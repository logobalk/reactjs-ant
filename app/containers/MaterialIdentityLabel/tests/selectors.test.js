// import { fromJS } from 'immutable';
// import { selectMaterialIdentityLabelDomain } from '../selectors';

describe('selectMaterialIdentityLabelDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
