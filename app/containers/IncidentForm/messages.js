/*
 * IncidentForm Messages
 *
 * This contains all the text for the IncidentForm container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.IncidentForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Sampling Material',
  },
});
