// import { fromJS } from 'immutable';
// import { selectDispensingMaterialGroupListDomain } from '../selectors';

describe('selectDispensingMaterialGroupListDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
