/*
 * DispensingMaterialGroupList Messages
 *
 * This contains all the text for the DispensingMaterialGroupList container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DispensingMaterialGroupList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Dispensing Material Group',
  },
});
