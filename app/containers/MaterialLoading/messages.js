/*
 * MaterialLoading Messages
 *
 * This contains all the text for the MaterialLoading container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MaterialLoading';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Material Loading Process',
  },
});
