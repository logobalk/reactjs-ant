/*
 * DispensingMaterialGroup Messages
 *
 * This contains all the text for the DispensingMaterialGroup container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DispensingMaterialGroup';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Dispensing Material Group',
  },
});
