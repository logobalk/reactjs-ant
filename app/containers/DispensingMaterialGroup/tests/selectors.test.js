// import { fromJS } from 'immutable';
// import { selectDispensingMaterialGroupDomain } from '../selectors';

describe('selectDispensingMaterialGroupDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
