// import { fromJS } from 'immutable';
// import { selectDispensingAdjustmentDomain } from '../selectors';

describe('selectDispensingAdjustmentDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
