/*
 * DispensingSummarizeMaterial Messages
 *
 * This contains all the text for the DispensingSummarizeMaterial container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.DispensingSummarizeMaterial';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Summarize Material Data',
  },
});
