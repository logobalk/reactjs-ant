// import { fromJS } from 'immutable';
// import { selectDispensingSummarizeMaterialDomain } from '../selectors';

describe('selectDispensingSummarizeMaterialDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
