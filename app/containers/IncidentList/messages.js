/*
 * IncidentList Messages
 *
 * This contains all the text for the IncidentList container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.IncidentList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the IncidentList container!',
  },
});
