/*
 * SummarizeMaterial Messages
 *
 * This contains all the text for the SummarizeMaterial container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SummarizeMaterial';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SummarizeMaterial container!',
  },
});
