// import { fromJS } from 'immutable';
// import { selectSummarizeMaterialStoreDomain } from '../selectors';

describe('selectSummarizeMaterialStoreDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
