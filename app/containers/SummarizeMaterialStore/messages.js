/*
 * SummarizeMaterialStore Messages
 *
 * This contains all the text for the SummarizeMaterialStore container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.SummarizeMaterialStore';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Summarize Material for Store',
  },
});
