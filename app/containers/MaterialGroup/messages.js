/*
 * MaterialGroup Messages
 *
 * This contains all the text for the MaterialGroup container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MaterialGroup';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Material Group Detail',
  },
});
