/*
 * InspectionReport Messages
 *
 * This contains all the text for the InspectionReport container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.InspectionReport';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the InspectionReport container!',
  },
});
