/*
 * WeighingModalExample Messages
 *
 * This contains all the text for the WeighingModalExample container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.WeighingModalExample';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Weighing Modal Example',
  },
});
