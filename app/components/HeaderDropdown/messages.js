/*
 * HeaderDropdown Messages
 *
 * This contains all the text for the HeaderDropdown component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.HeaderDropdown';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HeaderDropdown component!',
  },
});
