/*
 * AuditTrailHeader Messages
 *
 * This contains all the text for the AuditTrailHeader component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.AuditTrailHeader';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Add user',
  },
});
