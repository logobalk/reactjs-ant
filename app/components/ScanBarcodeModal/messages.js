/*
 * ScanBarcodeModal Messages
 *
 * This contains all the text for the ScanBarcodeModal component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ScanBarcodeModal';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Barcode Scanning',
  },
});
