/*
 * Weighting Messages
 *
 * This contains all the text for the Weighting component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Weighting';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Weighting Process',
  },
});
